namespace Core
{
    public enum EventId
    {
        OnHasError,
        OnProcessDownloadStart,
        OnProcessDownloadProgress,
        OnProcessDownloadCompleted,

        OnGetLinkByHttp,
    }
}
